<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusableInTouchMode="true"
    android:orientation="vertical">

    <LinearLayout
        android:padding="30dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <com.zt.yavon.widget.CustomEditText
            android:id="@+id/et_account"
            android:layout_marginTop="30dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:iconDrawable="@drawable/selector_account"
            app:drawablePadding="10dp"
            app:customHint="账号/手机号/邮箱"
            app:textSize="14sp" />
        <com.zt.yavon.widget.CustomEditText
            android:id="@+id/et_pwd"
            android:layout_marginTop="30dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:iconDrawable="@drawable/selector_password"
            app:visibleDrawable="@drawable/selector_password_visible"
            app:drawablePadding="10dp"
            app:customHint="密码"
            app:textSize="14sp" />
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp">
            <TextView
                android:drawablePadding="3dp"
                android:drawableLeft="@drawable/selector_login"
                android:id="@+id/tv_auto_login"
                android:padding="5dp"
                android:textColor="@color/mainGray"
                android:text="自动登录"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
            <TextView
                android:id="@+id/tv_forget_psd"
                android:padding="5dp"
                android:textColor="@color/mainGray"
                android:layout_alignParentRight="true"
                android:text="忘记密码"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </RelativeLayout>
        <TextView
            android:id="@+id/tv_login"
            android:padding="10dp"
            android:layout_marginTop="30dp"
            android:textColor="@color/mainGreen"
            android:gravity="center"
            android:background="@drawable/shape_stroke_green"
            android:text="登           录"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>
